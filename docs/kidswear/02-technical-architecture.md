# سند ۲ — معماری فنی روی Hostinger (+ لایه ایران)

## ۲٫۱ تصمیم بنیادین: چرا معماری «هیبرید» و نه فقط Hostinger؟

شما گفتید سایت روی Hostinger باشد. این درست است، ولی برای **فروش داخل ایران** به‌تنهایی کافی نیست. دلایل:

| مسئله | توضیح | راه‌حل |
|---|---|---|
| اینماد و اعتماد | فرآیند اینماد و درگاه بانکی با دامنه `.ir` و در دسترس بودن پایدار داخل ایران راحت‌تر پیش می‌رود | دامنه `.ir` روی لایه ایران، اپ روی Hostinger |
| قطعی اینترنت بین‌الملل | در بحران‌های اینترنتی، سایت‌های خارجی برای کاربر داخلی از دسترس خارج می‌شوند | آینه (Mirror) سبک روی هاست ایران + CDN داخلی |
| سرعت برای کاربر ایرانی | پینگ به اروپا ۸۰-۲۰۰ms و در ساعات پیک بدتر | CDN ابرآروان جلوی سایت، کش تهاجمی صفحات محصول |
| درگاه پرداخت و پیامک | برخی درگاه‌ها و پنل‌های پیامک IP خارجی را محدود می‌کنند | Relay ایرانی: کال‌بک پرداخت و ارسال پیامک از سرور ایران |
| دسترسی به AgentRouter و شبکه‌های خارجی | از سرور ایران مستقیم ممکن نیست | همه پردازش AI و انتشار خارجی روی Hostinger انجام شود |

> **جمع‌بندی:** Hostinger = مغز (اپلیکیشن، AI، اتوماسیون، انتشار جهانی). لایه ایران = چهره و دست (CDN، پرداخت، پیامک، دسترس‌پذیری بحران).

## ۲٫۲ دیاگرام معماری

```
                         کاربر ایرانی
                              │
                    ┌─────────▼──────────┐
                    │  CDN ابرآروان/ایران │  ← WAF، ضد DDoS، کش تصویر و صفحه
                    │   دامنه: shop.ir    │
                    └─────────┬──────────┘
                              │
             ┌────────────────┴───────────────┐
             │                                │
   ┌─────────▼──────────┐          ┌──────────▼───────────┐
   │  IRAN RELAY (VPS    │          │  HOSTINGER VPS (KVM2/4)│
   │  کوچک ایران)        │◄────────►│  Ubuntu 24.04 + Docker │
   │                     │  tunnel   │                        │
   │ • کال‌بک درگاه پرداخت│           │ ┌────────────────────┐ │
   │ • ارسال پیامک       │           │ │ Next.js 16 (اپ اصلی)│ │
   │ • آینه استاتیک بحران │           │ ├────────────────────┤ │
   │ • مانیتورینگ داخلی   │           │ │ MySQL / MariaDB     │ │
   └─────────────────────┘           │ ├────────────────────┤ │
                                     │ │ Redis (کش/صف)       │ │
   شبکه‌های اجتماعی ◄───────────────► │ ├────────────────────┤ │
   IG / TG / Bale / Eitaa /          │ │ MinIO (تصویر/ویدئو) │ │
   Rubika / WhatsApp / Pinterest /   │ ├────────────────────┤ │
   YouTube / Aparat / LinkedIn       │ │ n8n (ارکستراتور)    │ │
                                     │ ├────────────────────┤ │
   AgentRouter ◄─────────────────────┤ │ Worker: FFmpeg/Sharp│ │
   (متن، توضیح، کپشن، سناریو)         │ ├────────────────────┤ │
                                     │ │ Coolify / Portainer │ │
                                     │ └────────────────────┘ │
                                     └────────────────────────┘
```

## ۲٫۳ انتخاب پلن Hostinger

بر اساس نیاز واقعی این پروژه (Next.js + MySQL + Redis + MinIO + n8n + رندر ویدئو):

| پلن | مشخصات | مناسب برای |
|---|---|---|
| KVM 1 — ۱ vCPU / ۴GB / ۵۰GB NVMe | حداقلی | فقط تست و توسعه |
| **KVM 2 — ۲ vCPU / ۸GB / ۱۰۰GB NVMe** | **شروع توصیه‌شده** | تا ~۵۰۰ محصول و ~۳۰ پست روزانه |
| **KVM 4 — ۴ vCPU / ۱۶GB / ۲۰۰GB NVMe** | **از ماه ۴** | رندر هم‌زمان ویدئو + n8n سنگین + ترافیک بالا |
| KVM 8 — ۸ vCPU / ۳۲GB | مقیاس بزرگ | وقتی مارکت‌پلیس شدید |

قیمت‌ها حدود ۶٫۵ تا ۲۶ دلار در ماه هستند و پلن KVM 2 برای اکثر بارهای کاری اتوماسیون کافی است ([مرجع](https://ivristech.com/n8n-self-hosting-hostinger/), [مرجع](https://hostadvice.com/hosting-company/hostinger-reviews/n8n-hosting-review/)). Hostinger برای n8n تصویر آماده هم دارد که نصب را ساده می‌کند.

**پیشنهاد نهایی:** KVM 2 با تعهد بلندمدت (قیمت خیلی بهتر)، و ارتقا به KVM 4 در ماه ۴.

## ۲٫۴ استک نرم‌افزاری

| لایه | انتخاب | دلیل |
|---|---|---|
| فریم‌ورک | **Next.js 16 (App Router)** | ادامه استک `ehsansalehi.ir` — کد auth، db، socialPoster، watermark، i18n قابل بازاستفاده مستقیم است |
| زبان | TypeScript | همان استک |
| UI | Tailwind + Framer Motion | همان استک، RTL-first |
| دیتابیس | MySQL 8 / MariaDB | `app/lib/mysql.ts` و `app/lib/db/index.ts` آماده است |
| کش و صف | Redis + BullMQ | صف کارهای اتوماسیون (پردازش تصویر، رندر ویدئو، انتشار) |
| ذخیره فایل | MinIO (S3-compatible) | تصاویر محصول، ویدئوها، بکاپ |
| ارکستراسیون | **n8n** self-hosted | سناریوهای بازاریابی/انتشار قابل ویرایش بدون کدنویسی |
| موتور AI | **AgentRouter** (Anthropic-compatible و OpenAI-compatible) | تولید متن، توضیح محصول، کپشن، سناریوی ویدئو |
| تصویر | Sharp + rembg (حذف پس‌زمینه) | پردازش سریع محلی |
| ویدئو | FFmpeg + قالب‌های آماده | ساخت Reels/Shorts از عکس‌ها |
| جستجو | MeiliSearch | جستجوی فارسی سریع با تحمل غلط املایی |
| مانیتورینگ | Uptime Kuma + Grafana/Loki | آپتایم و لاگ |
| استقرار | Docker Compose + Coolify | دیپلوی گیت‌محور، شبیه Vercel |
| CI/CD | GitHub Actions | همان الگوی `deploy-mizbanfa.yml` ولی برای VPS |

## ۲٫۵ اتصال به AgentRouter

AgentRouter دو حالت سازگاری دارد:

```bash
# حالت OpenAI-compatible — مناسب برای کد فعلی شما که از پکیج openai استفاده می‌کند
OPENAI_BASE_URL=https://agentrouter.org/v1
OPENAI_API_KEY=<AgentRouter API Key>
OPENAI_MODEL=claude-opus-4-6

# حالت Anthropic-compatible — مخصوص Claude Code CLI (بدون /v1)
ANTHROPIC_BASE_URL=https://agentrouter.org
ANTHROPIC_AUTH_TOKEN=<AgentRouter API Key>
ANTHROPIC_MODEL=claude-opus-4-6
```

نکته مهم: مستندات AgentRouter تأکید می‌کند Base URL حالت Claude Code **نباید** `/v1` داشته باشد و نباید با حالت OpenAI-compatible قاطی شود.

**خبر خوب:** پروژه فعلی شما همین الان `OPENAI_BASE_URL` و `OPENAI_MODEL` را می‌خواند، پس اتصال به AgentRouter بدون تغییر کد ممکن است.

**نکته حیاتی:** AgentRouter برای متن/کدنویسی است. برای **تولید و ویرایش تصویر** و **تولید ویدئو** به سرویس جداگانه نیاز داریم (جزئیات در سند ۳ و ۸).

**لایه انتزاع اجباری:** یک ماژول `lib/ai/provider.ts` می‌نویسیم که همه فراخوانی‌های AI از آن رد شود، با:
- انتخاب مدل بر اساس نوع کار (ارزان برای کپشن، قوی برای مقاله)
- Fallback خودکار به provider دوم اگر AgentRouter پاسخ نداد
- کش پاسخ‌های تکراری در Redis (کاهش هزینه تا ۴۰٪)
- سقف مصرف روزانه و لاگ هزینه

## ۲٫۶ ساختار سرویس‌ها (Docker Compose)

```yaml
# طرح مفهومی — نسخه نهایی در فاز اجرا
services:
  app:          # Next.js standalone
  worker:       # BullMQ worker: image, video, publish
  mysql:
  redis:
  minio:
  meilisearch:
  n8n:
  caddy:        # reverse proxy + TLS خودکار
  uptime-kuma:
  backup:       # cron: mysqldump + rclone به ابرآروان و Backblaze
```

## ۲٫۷ ساختار پوشه‌بندی اپ

```
app/
  (shop)/                    # فروشگاه عمومی
    page.tsx                 # صفحه اصلی
    products/[slug]/         # صفحه محصول
    category/[...slug]/      # دسته‌بندی (پسرانه/دخترانه/سن/فصل)
    cart/ checkout/ track/   # سبد، پرداخت، رهگیری
    blog/[slug]/             # بلاگ سئویی
    size-guide/              # راهنمای سایز هوشمند
  (seller)/                  # پنل فروشنده — PWA
    upload/                  # ★ آپلود عکس (قلب اتوماسیون)
    products/ orders/ stats/
  (admin)/                   # پنل مدیریت
    automation/ social/ seo/ ads/ inventory/
  api/
    ai/                      # پروکسی AgentRouter
    ingest/photos/           # ★ ورودی عکس‌ها
    products/ orders/ payment/
    social/publish/          # انتشار چندکاناله
    cron/                    # زمان‌بندها
    webhooks/                # درگاه، پیک، اینستاگرام
lib/
  ai/ media/ social/ seo/ commerce/ db/
```

## ۲٫۸ امنیت

- Caddy با TLS خودکار، HSTS، و هدرهای امنیتی؛ WAF ابرآروان جلوی آن
- SSH فقط با کلید، پورت غیر پیش‌فرض، fail2ban، UFW
- جداسازی کامل کلیدها: هیچ کلیدی در گیت — فقط `.env` روی سرور با دسترسی ۶۰۰
- نقش‌های کاربری: `owner / admin / seller / support / viewer` با RBAC
- رمزنگاری توکن‌های شبکه‌های اجتماعی در دیتابیس (AES-256 با کلید در env)
- Rate limit روی API عمومی و لاگین (Redis-based)
- لاگ حسابرسی (audit log) برای هر تغییر قیمت، موجودی و انتشار
- بکاپ ۳-۲-۱: روزانه روی VPS، شبانه روی ابرآروان، هفتگی روی Backblaze B2؛ تست بازیابی ماهانه

## ۲٫۹ عملکرد (Performance)

هدف: **LCP زیر ۲٫۵ ثانیه روی موبایل ایرانی با اینترنت متوسط**.

- ISR برای صفحات محصول و دسته (revalidate ۶۰ ثانیه)
- تصاویر: WebP/AVIF، سه سایز، `next/image` + کش CDN طولانی
- فونت فارسی محلی (وزیرمتن/ایران‌سنس) با `font-display: swap`
- JS اولیه زیر ۱۵۰KB؛ کامپوننت‌های سنگین با dynamic import
- Skeleton برای همه صفحات (الگوی `loading.tsx` که در پروژه فعلی دارید)
- بودجه عملکرد در CI: هر PR که Lighthouse را زیر ۹۰ ببرد، fail شود

## ۲٫۱۰ برنامه تداوم کسب‌وکار (قطعی اینترنت)

سه سطح تخریب کنترل‌شده:

1. **سطح سبز (عادی):** همه‌چیز روی Hostinger، CDN ایران جلو.
2. **سطح زرد (کندی بین‌الملل):** CDN از کش سرو می‌کند؛ صفحات محصول تا ۲۴ ساعت از کش. سفارش‌گیری از Relay ایران با صف آفلاین.
3. **سطح قرمز (قطع کامل بین‌الملل):** آینه ایرانی فعال می‌شود — نسخه استاتیک کاتالوگ + فرم سفارش + پرداخت داخلی. اتوماسیون خارجی متوقف، کانال‌های داخلی (بله/ایتا/روبیکا/پیامک) فعال می‌مانند. همگام‌سازی سفارش‌ها پس از بازگشت اتصال.

هاست‌های ایرانی در دوره‌های محدودیت شدید اینترنت به‌مراتب در دسترس‌تر بوده‌اند ([مرجع](https://mizbanfa.net/))، به همین دلیل لایه ایران در معماری «اختیاری» نیست.

## ۲٫۱۱ چه چیزی از پروژه فعلی مستقیماً بازاستفاده می‌شود؟

| فایل/ماژول فعلی | استفاده جدید |
|---|---|
| `app/lib/socialPoster.ts` | هسته انتشار چندکاناله — فقط قالب کپشن عوض می‌شود |
| `app/lib/linkedinPoster.ts` | کانال B2B |
| `app/lib/watermark.ts` | واترمارک برند روی عکس محصول |
| `app/lib/mysql.ts` + `app/lib/db/index.ts` | لایه دیتابیس با fallback هوشمند env |
| `app/lib/auth.ts` + `middleware.ts` | احراز هویت و محافظت پنل |
| `app/api/admin/automation/route.ts` | جدول `automation_settings` و مدیریت کلیدها از پنل |
| `app/api/admin/integrations-test/route.ts` | تست سلامت اتصال‌ها |
| `app/api/cron/*` | الگوی cron با `CRON_SECRET` |
| `app/components/AiChatbot.tsx` | چت‌بات فروش و مشاور سایز |
| `app/lib/i18n.ts` + RTL | چندزبانه (فا/عر/en) |
| `scripts/*` و `deploy.sh` | الگوی دیپلوی، با تطبیق برای Docker |

این یعنی تقریباً **۳۵ تا ۴۵ درصد کد پایه از قبل آماده است** — مزیت بزرگ زمانی و هزینه‌ای.
