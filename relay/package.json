{
  "name": "social-relay",
  "version": "1.0.1",
  "private": true,
  "description": "رله انتشار شبکه‌های اجتماعی — روی هاستینگر اجرا می‌شود تا محدودیت IP ایران دور زده شود",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo 'No build step required for the relay'",
    "postinstall": "echo 'No dependencies to install'",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "health": "curl -s http://localhost:${PORT:-3001}/health"
  },
  "keywords": [
    "relay",
    "proxy",
    "social"
  ],
  "dependencies": {},
  "devDependencies": {}
}
