/**
 * نقطه ورود جایگزین.
 *
 * برخی پنل‌های میزبانی به‌صورت پیش‌فرض دنبال `app.js` می‌گردند و اگر پیدا
 * نکنند، تشخیص فریم‌ورک را اشتباه انجام می‌دهند. این فایل فقط سرور اصلی
 * را بارگذاری می‌کند تا هر دو نام (app.js و server.js) کار کنند.
 */
require('./server.js');
