پرامپت فروشگاه پوشاک کودک — دیجی‌کالا + پرو آنلاین
نقش: مهندس ارشد فول‌استک Next.js/TS/MySQL.
مرجع: _ARENA_DELIVERABLES/ (۰۱ بیزینس، ۰۲ معماری، ۰۳ اتوماسیون، ۰۵ سئو، ۰۸ env، ۰۹ دیتابیس، ۱۰ تصمیمات، ۱۱ پرو، ۱۲ راه‌حل) و app/lib/(auth، mysql، socialPoster، relayClient، i18n) را توسعه بده نه بازنویسی.
استک روز: Next.js نصب‌شده (RSC+ServerActions+ISR)؛ TS strict؛ Tailwind+shadcn/ui+Radix+Framer Motion؛ TanStack Query+Zustand؛ RHF+zod؛ mysql2 پارامتری؛ next/image+font؛ PWA؛ a11y؛ CWV≥۹۵. RTL درجه‌یک: راست، وزیرمتن+ارزشمند، اعداد fa-IR. تومان.
دیتابیس: 09→migration+seed۳۰محصول؛ reviews+size_fit.
مشتری (=دیجی‌کالا): خانه؛ دسته URL فارسی+فیلتر(جنسیت/سن/سایز/رنگ/قیمت)+مرتب‌سازی؛ جستجوی زنده؛ محصول(گالری، سایز×رنگ+موجودی، جدول سایز، نظرات size_fit، FAQ، مرتبط، JSON-LD)؛ تسویه(آدرس/ارسال/کوپن/درگاه sandbox)؛ اکانت(OTP، سفارش/رهگیری/مرجوعی، علاقه، پروفایل کودک)؛ بلاگ AI؛ قوانین.
⭐پرو(۱۱): ۱) سایز هوشمند: قد/وزن/سن/جنسیت → سایز از قد+تعدیل وزن/fit_type/نظرات/«برای رشد» → اطمینان٪+شبح. ۲) آواتار 2D Canvas + لایه tryon_assets + ست + اشتراک. پروفایل کودک اختیاری. سطح۳ ممنوع.
ادمین: KPI، محصول/سفارش/مشتری/کوپن/بلاگ/نظر، اکسل، RBAC، آمار پرو، صف.
اتوماسیون: POST /api/ingest/photos (PWA/تلگرام اول/پنل)→پردازش تصویر(پس‌زمینه، چندسایزی، واترمارک، WebP، لایه پرو)→Vision(JSON، زیر۰.۷→بازبینی)→SKU/قیمت→عنوان/توضیح/متا/FAQ+hash ضدتکرار→انتشار ۱۰ شبکه(retry/Review). صف: job_queue+کرون؛ ویدئو: مرورگر.
سئو: sitemap جدا، IndexNow، canonical، noindex، breadcrumb، ۴۰۴، Article/HowTo، OG.
امنیت: کلید فقط .env؛ حریم کودک خط قرمز؛ zod؛ SQL پارامتری؛ rate-limit؛ HMAC؛ بی‌کلید→mock.
دیپلوی: فاز۱ بی‌VPS (cPanel)؛ Actions→FTP میزبان‌فا+SSH هاستینگر؛ /api/health.
فازها: S0 پایه+RTL+فونت+DB، S1 کاتالوگ، S2 خرید، S3 اکانت/قانونی، S4 ادمین، S5 پرو، S6 اتوماسیون، S7 رشد(بلاگ/باشگاه/چت). هر فاز: build+تست.
تمام‌شده: build/lint+تست، فارسی+RTL، mock، بی‌کلید، مستند، گزارش فارسی(ساخته/معوق/تصمیمات/قدم). ضد: ووکامرس، بازنویسی سالم، تغییر برنچ، کلید/عکس کودک در گیت، باینری.